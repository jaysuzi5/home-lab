apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - values.yaml
  - sealed-kafka-ui-secret.yaml
namespace: kafka-ui
configMapGenerator:
  - name: values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
