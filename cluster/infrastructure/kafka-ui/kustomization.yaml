apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
namespace: kafka-ui
configMapGenerator:
  - name: kafka-ui-values
    files:
      - values.yaml
    options:
      disableNameSuffixHash: true
